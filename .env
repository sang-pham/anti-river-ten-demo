# Example environment configuration
PORT=8081
LOG_LEVEL=info
REQUEST_TIMEOUT=30s
MAX_BODY_BYTES=1048576
# Comma-separated list (no spaces) or wildcard *
# ALLOWED_ORIGINS=http://localhost:3000,https://example.com

# Database and Auth
# PostgreSQL DSN format:
#   postgres://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=disable
# Example for local dev:
DATABASE_URL=postgres://postgres:postgres@postgres:5432/go_demo?sslmode=disable

# JWT settings
# Use a strong, random secret in production
JWT_SECRET=-agrcKBGjR3HfmSJUzuUfZ9WJoLSLsdfi8Y/lAFeCnAM=
# Token time-to-live (Go duration format, e.g., 24h, 15m)
JWT_TTL=1h
REFRESH_TTL=24h
